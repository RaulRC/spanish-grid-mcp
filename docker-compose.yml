services:
  spanish-grid:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - spanish-grid-cache:/data/cache
    restart: unless-stopped

volumes:
  spanish-grid-cache:
